# LLM-WIKI-RY Agent 维护规则

本仓库是由 Agent 协助维护的个人 LLM Wiki。Markdown 是长期知识资产；原始来源、派生知识和可再生发布产物必须保持边界。

## 开始前

1. 先读 `README.md` 和 `CONTEXT.md`，再按索引进入相关页面。
2. 检查 `git status --short`；已有改动属于用户，不得覆盖、清理或顺手格式化。
3. 明确本次操作属于 `ingest`、`query`、`lint`、`index` 或 `bootstrap`。
4. 只修改与当前目标直接相关的受管路径；`.artifacts/` 和 registry 的排除路径不属于 Wiki 事实层。

## 权威顺序

发生冲突时按以下顺序判断：

1. 用户本次提供且可以核验的原始来源；
2. `research/` 中有明确证据范围的研究笔记；
3. `content/` 中通过 Harness 的正式内容；
4. `drafts/` 中尚未正式化的内容。

通过结构检查只说明页面符合仓库约定，不代表其中所有事实已经核验。生成页面之间的互相引用不能替代原始来源。

## 操作协议

### Ingest

- 默认一次只摄取一个来源；批量摄取必须先说明来源数、影响页面和失败恢复方式。
- 记录标题、责任人或作者、版本或日期、原始链接或本地路径以及可核验范围。
- 先形成 `research/` 证据笔记，把来源事实、分析判断和证据不足的问题分开。
- 只有用户明确要求形成长期文章时，才进入 `drafts/` 或 `content/`。
- 新增或正式化页面时同步更新 `README.md`；不得只根据文件名决定落点。

### Query

- 先读 `README.md`，再读取最相关页面；不要无差别加载全库。
- 回答时引用具体页面或原始来源，并保留状态、日期、适用范围和限制条件。
- “Wiki 没写”不等于“事实不存在”。与更高权威来源冲突时，以更高权威来源为准，并指出 Wiki 可能过期。
- 查询默认只回答，不回写。只有用户明确要求沉淀时才更新 Wiki。

### Lint

- 先运行 `npm run check`，再检查重复内容、明显矛盾、术语漂移、缺少来源、陈旧信息和未关闭问题。
- 结果分为确定性错误、疑似语义问题和结构建议。
- 只报告语义冲突并提出方案，不自动裁决事实。

### Index

- `README.md` 是唯一总入口，不创建平行总索引。
- 每个索引项只写一句能够帮助检索的摘要。
- `docs/wiki-activity.md` 只记录摄取、正式化、lint 和结构调整，不记录普通查询，也不作为事实证据。

## 研究状态

研究笔记在一级标题后使用以下字段：

```text
> 状态：verified | partial | draft | stale
> 证据日期：YYYY-MM-DD
> 适用范围：一句话说明时间、对象和口径
```

- `verified`：页面结论在标注范围内已由当前可核验证据支持。
- `partial`：只有部分结论或范围得到支持，缺口必须明确。
- `draft`：仍在整理，不能作为稳定结论。
- `stale`：来源、时间或适用条件已漂移，需要复核。

状态描述的是证据充分度，不描述文章写作质量。

## 不可突破的边界

- 不修改原始来源，除非用户明确要求修改来源本身。
- 不删除或重命名文件，不执行 `git commit`、`git push` 或创建合并请求，除非用户另行明确授权。
- 不把发布载荷、预览、回读或生成缓存当作权威内容。
- 不为增加链接密度创建没有阅读价值的交叉链接。

## 完成回执

回执必须包含：实际改动范围、运行的校验、关键文件回读结果、仍未证明的事项，以及是否执行过提交或远端同步。
